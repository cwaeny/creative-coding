function setup() {
  createCanvas(600, 600);
  background(color(244, 243, 241));
}

function draw() {
  noFill();
  stroke(234, 87, 103);

  let newSize = 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);

  newSize += 25;
  circle(300, 300, newSize);
}
